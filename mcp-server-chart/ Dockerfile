# 使用一个轻量级的官方 Node.js 镜像作为基础
FROM node:20-alpine

# 安装 @antv/mcp-server-chart 包
RUN npm install -g @antv/mcp-server-chart

# 暴露服务运行的端口
EXPOSE 1122

# 容器启动时执行的默认命令 (使用 SSE)
CMD ["mcp-server-chart", "--transport", "sse"]